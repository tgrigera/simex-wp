<?xml version="1.0" encoding="utf-8"  standalone="no"?>
<svg  onload="if (typeof(gnuplot_svg)!='undefined') gnuplot_svg.Init(evt)" 
 viewBox="0 0 600 480"
 xmlns="http://www.w3.org/2000/svg"
 xmlns:xlink="http://www.w3.org/1999/xlink"
>

<title>Gnuplot</title>
<desc>Produced by GNUPLOT 5.4 patchlevel 1 </desc>

<script type="text/javascript" > <![CDATA[
// From:	Marko Karjalainen <markokarjalainen@kolumbus.fi>
// Date:	27 Aug 2018
// Experimental gnuplot plugin for svg
//
// All svg elements on page get own gnuplot plugin attached by js, so no conflict with global variables.
//
// Javascript variables are read from second script tag and converted to json for import to plugin.
// Inline events are removed from xml and new ones are attached with addEventListener function.
// Inline events should be removed from xml and xml should have better id/class names to attach events from js.
//
// Improved mouseover text and image handling
//   content changed to xml only if it really changed and bouncing is calculated once.
//
// Convert functions are same as before, maybe renamed better.
//
// Javascript routines for mouse and keyboard interaction with
// SVG documents produced by gnuplot SVG terminal driver.

// TODO do not create inline events to svg and give id or classes for getting elements
// TODO make own svg layer x/y range sized for coordinates?

if (window) {
    window.addEventListener('load', function () {
        // Find svg elements
        var svg = document.querySelectorAll('svg');
        for (var i = 0; i < svg.length; i++) {
            // Init plugin
            if (!svg[i].gnuplot) {
                // Check if gnuplot generated svg
                if(svg[i].getElementById('gnuplot_canvas')){
                    svg[i].gnuplot = new gnuplot_svg(svg[i]);
                }
            }
        }
    });
}

gnuplot_svg = function (svgElement) {

    var version = '09 April 2019';

    var settings = {};

    var viewBoxResetValue = [];

    var drag = {
        'enabled': false,
        'offset': { 'x': 0, 'y': 0 },
        'change': svgElement.createSVGPoint(),
        'timeout': null
    };

    var coordinateText = {
        'enabled': false,
        'element': svgElement.getElementById('coord_text')
    };

    var popoverContainer = {
        'element': null,
        'content': null,
    };

    var popoverImage = {
        'element': null,
        'content': null,
        'width': 300,
        'height': 200,
        'defaultWidth': 300,
        'defaultHeight': 200,
    };

    var popoverText = {
        'element': null,
        'content': null,
        'width': 11,
        'height': 16,
        'defaultWidth': 11,
        'defaultHeight': 16,
    };

    var point = svgElement.createSVGPoint();

    var axisDate = new Date();

    var gridEnabled = false;

    // Get plot boundaries and axis scaling information for mousing from current object script tag
    // TODO add these to svg xml custom attribute for reading(json format)
    var parseSettings = function () {
        var script = svgElement.querySelectorAll('script');
        if (script && script[1]) {
            var scriptText = script[1].firstChild.nodeValue;
            // Remove inline comments
            scriptText = scriptText.replace(/^\s*\/\/.*\n/g, '');
            // Change prefix to "
            scriptText = scriptText.replace(/gnuplot_svg\./g, '"');
            // Change = to " :
            scriptText = scriptText.replace(/ = /g, '" : ');
            // Change line endings to comma
            scriptText = scriptText.replace(/;\n|\n/g, ',');
            // Remove last comma
            scriptText = scriptText.replace(/,+$/, '');
            // Parse as json string
            settings = JSON.parse("{\n" + scriptText + "\n}");
        }
    };

    // Add interactive events
    var addEvents = function () {
        var i;

        // Get keyentry elements
        var toggleVisibility = svgElement.querySelectorAll('g[id$="_keyentry"]');
        for (i = 0; i < toggleVisibility.length; i++) {
            // ------- Remove inline events
            toggleVisibility[i].removeAttribute('onclick');
            // -------

            // Add keyentry event to toggle visibility
            toggleVisibility[i].addEventListener('click', key.bind(null, toggleVisibility[i].getAttribute('id'), null));
        }

        // ------- Remove inline events from bounding box
        var boundingBox = svgElement.querySelector('rect[onclick^="gnuplot_svg.toggleCoordBox"]');
        if (boundingBox) {
            boundingBox.removeAttribute('onclick');
            boundingBox.removeAttribute('onmousemove');
        }
        // ------- Remove inline events from canvas
        var canvas = svgElement.getElementById('gnuplot_canvas');
        if (canvas) {
            canvas.removeAttribute('onclick');
            canvas.removeAttribute('onmousemove');
        }
        // -------

        // Get grid image
        var toggleGrid = svgElement.querySelector('image[onclick^="gnuplot_svg.toggleGrid"]');
        if (toggleGrid) {
            // ------- Remove inline events
            toggleGrid.removeAttribute('onclick');
            // -------

            // Add Toggle grid image event
            toggleGrid.addEventListener('click', function (evt) {
                grid();
                evt.preventDefault();
                evt.stopPropagation();
            });
        }

        // Get hypertexts
        var hyperText = svgElement.querySelectorAll('g[onmousemove^="gnuplot_svg.showHypertext"]');

        // Set view element variables
        if (hyperText.length) {
            popoverContainer.element = svgElement.getElementById('hypertextbox');
            popoverText.element = svgElement.getElementById('hypertext');
            popoverImage.element = svgElement.getElementById('hyperimage');
            popoverImage.defaultWidth = popoverImage.element.getAttribute('width');
            popoverImage.defaultHeight = popoverImage.element.getAttribute('height');
        }

        for (i = 0; i < hyperText.length; i++) {
            // Get text from attr uggly way, svg has empty title element
            var text = hyperText[i].getAttribute('onmousemove').substr(31).slice(0, -2);

            // ------- Remove inline events
            hyperText[i].removeAttribute('onmousemove');
            hyperText[i].removeAttribute('onmouseout');
            // -------

            // Add event
            hyperText[i].addEventListener('mousemove', popover.bind(null, text, true));
            hyperText[i].addEventListener('mouseout', popover.bind(null, null, false));
        }

        // Toggle coordinates visibility on left click on boundingBox element
        svgElement.addEventListener('click', function (evt) {
            if (!drag.enabled) {
                // TODO check if inside data area, own layer for this is needed?
                coordinate();
                setCoordinateLabel(evt);
            }
        });

        // Save move start position, enable drag after delay
        svgElement.addEventListener('mousedown', function (evt) {

            drag.offset = { 'x': evt.clientX, 'y': evt.clientY };

            // Delay for moving, so not move accidentally if only click
            drag.timeout = setTimeout(function () {
                drag.enabled = true;
            }, 250);

            // Cancel draggable
            evt.stopPropagation();
            evt.preventDefault();
            return false;
        });

        // Disable drag
        svgElement.addEventListener('mouseup', function (evt) {
            drag.enabled = false;
            clearTimeout(drag.timeout);
        });

        // Mouse move
        svgElement.addEventListener('mousemove', function (evt) {

            // Drag svg element
            if (evt.buttons == 1 && drag.enabled) {

                // Position change
                drag.change.x = evt.clientX - drag.offset.x;
                drag.change.y = evt.clientY - drag.offset.y;

                // Set current mouse position
                drag.offset.x = evt.clientX;
                drag.offset.y = evt.clientY;

                // Convert to svg position
                drag.change.matrixTransform(svgElement.getScreenCTM().inverse());

                var viewBoxValues = getViewBox();

                viewBoxValues[0] -= drag.change.x;
                viewBoxValues[1] -= drag.change.y;

                setViewBox(viewBoxValues);
            }

            // View coordinates on mousemove over svg element
            if (coordinateText.enabled) {
                // TODO check if inside data area, own layer for this is needed?
                setCoordinateLabel(evt);
            }

        });

        // Zoom with wheel
        svgElement.addEventListener('wheel', function (evt) {
            // x or y scroll zoom both axels
            var delta = Math.max(-1, Math.min(1, (evt.deltaY || evt.deltaX)));

            if (delta > 0) {
                setViewBox(zoom('in'));
            }
            else {
                setViewBox(zoom('out'));
            }

            // Disable scroll the entire webpage
            evt.stopPropagation();
            evt.preventDefault();
            return false;
        });

        // Reset on right click or hold tap
        svgElement.addEventListener('contextmenu', function (evt) {

            setViewBox(viewBoxResetValue);

            // Disable native context menu
            evt.stopPropagation();
            evt.preventDefault();
            return false;
        });

        // Keyboard actions, old svg version not support key events so must listen window
        window.addEventListener('keydown', function (evt) {

            // Not capture event from inputs
            // body = svg inline in page, svg = plain svg file, window = delegated events to object
            if (evt.target.nodeName != 'BODY' && evt.target.nodeName != 'svg' && evt.target != window) {
                return true;
            }

            var viewBoxValues = [];

            switch (evt.key) {
                // Move, Edge sends without Arrow word
                case 'ArrowLeft':
                case 'Left':
                case 'ArrowRight':
                case 'Right':
                case 'ArrowUp':
                case 'Up':
                case 'ArrowDown':
                case 'Down':
                    viewBoxValues = pan(evt.key.replace('Arrow', '').toLowerCase());
                    break;
                // Zoom in
                case '+':
                case 'Add':
                    viewBoxValues = zoom('in');
                    break;
                // Zoom out
                case '-':
                case 'Subtract':
                    viewBoxValues = zoom('out');
                    break;
                // Reset
                case 'Home':
                    viewBoxValues = viewBoxResetValue;
                    break;
                // Toggle grid
                case '#':
                    grid();
                    break;
            }

            if (viewBoxValues.length) {
                setViewBox(viewBoxValues);
            }
        });
    };

    // Get svg viewbox details
    var getViewBox = function () {
        var viewBoxValues = svgElement.getAttribute('viewBox').split(' ');
        viewBoxValues[0] = parseFloat(viewBoxValues[0]);
        viewBoxValues[1] = parseFloat(viewBoxValues[1]);
        viewBoxValues[2] = parseFloat(viewBoxValues[2]);
        viewBoxValues[3] = parseFloat(viewBoxValues[3]);
        return viewBoxValues;
    };

    // Set svg viewbox details
    var setViewBox = function (viewBoxValues) {
        svgElement.setAttribute('viewBox', viewBoxValues.join(' '));
    };

    // Set coordinate label position and text
    var setCoordinateLabel = function (evt) {
        var position = convertDOMToSVG({ 'x': evt.clientX, 'y': evt.clientY });

        // Set coordinate label position
        coordinateText.element.setAttribute('x', position.x);
        coordinateText.element.setAttribute('y', position.y);

        // Convert svg position to plot coordinates
        var plotcoord = convertSVGToPlot(position);

        // Parse label to view
        var label = parseCoordinateLabel(plotcoord);

        // Set coordinate label text
        coordinateText.element.textContent = label.x + ' ' + label.y;
    };

    // Convert position DOM to SVG
    var convertDOMToSVG = function (position) {
        point.x = position.x;
        point.y = position.y;
        return point.matrixTransform(svgElement.getScreenCTM().inverse());
    };

    // Convert position SVG to Plot
    var convertSVGToPlot = function (position) {
        var plotcoord = {};
        var plotx = position.x - settings.plot_xmin;
        var ploty = position.y - settings.plot_ybot;
        var x, y;

        if (settings.plot_logaxis_x !== 0) {
            x = Math.log(settings.plot_axis_xmax)
                - Math.log(settings.plot_axis_xmin);
            x = x * (plotx / (settings.plot_xmax - settings.plot_xmin))
                + Math.log(settings.plot_axis_xmin);
            x = Math.exp(x);
        } else {
            x = settings.plot_axis_xmin + (plotx / (settings.plot_xmax - settings.plot_xmin)) * (settings.plot_axis_xmax - settings.plot_axis_xmin);
        }

        if (settings.plot_logaxis_y !== 0) {
            y = Math.log(settings.plot_axis_ymax)
                - Math.log(settings.plot_axis_ymin);
            y = y * (ploty / (settings.plot_ytop - settings.plot_ybot))
                + Math.log(settings.plot_axis_ymin);
            y = Math.exp(y);
        } else {
            y = settings.plot_axis_ymin + (ploty / (settings.plot_ytop - settings.plot_ybot)) * (settings.plot_axis_ymax - settings.plot_axis_ymin);
        }

        plotcoord.x = x;
        plotcoord.y = y;
        return plotcoord;
    };

    // Parse plot x/y values to label
    var parseCoordinateLabel = function (plotcoord) {
        var label = { 'x': 0, 'y': 0 };

        if (settings.plot_timeaxis_x == 'DMS' || settings.plot_timeaxis_y == 'DMS') {
            if (settings.plot_timeaxis_x == 'DMS') {
                label.x = convertToDMS(plotcoord.x);
            }
            else {
                label.x = plotcoord.x.toFixed(2);
            }

            if (settings.plot_timeaxis_y == 'DMS') {
                label.y = convertToDMS(plotcoord.y);
            }
            else {
                label.y = plotcoord.y.toFixed(2);
            }

        } else if (settings.polar_mode) {
            polar = convertToPolar(plotcoord.x, plotcoord.y);
            label.x = 'ang= ' + polar.ang.toPrecision(4);
            label.y = 'R= ' + polar.r.toPrecision(4);

        } else if (settings.plot_timeaxis_x == 'Date') {
            axisDate.setTime(1000 * plotcoord.x);
            var year = axisDate.getUTCFullYear();
            var month = axisDate.getUTCMonth();
            var date = axisDate.getUTCDate();
            label.x = (' ' + date).slice(-2) + '/' + ('0' + (month + 1)).slice(-2) + '/' + year;
            label.y = plotcoord.y.toFixed(2);
        } else if (settings.plot_timeaxis_x == 'Time') {
            axisDate.setTime(1000 * plotcoord.x);
            var hour = axisDate.getUTCHours();
            var minute = axisDate.getUTCMinutes();
            var second = axisDate.getUTCSeconds();
            label.x = ('0' + hour).slice(-2) + ':' + ('0' + minute).slice(-2) + ':' + ('0' + second).slice(-2);
            label.y = plotcoord.y.toFixed(2);
        } else if (settings.plot_timeaxis_x == 'DateTime') {
            axisDate.setTime(1000 * plotcoord.x);
            label.x = axisDate.toUTCString();
            label.y = plotcoord.y.toFixed(2);
        } else {
            label.x = plotcoord.x.toFixed(2);
            label.y = plotcoord.y.toFixed(2);
        }

        return label;
    };

    // Convert position to Polar
    var convertToPolar = function (x, y) {
        polar = {};
        var phi, r;
        phi = Math.atan2(y, x);
        if (settings.plot_logaxis_r) {
            r = Math.exp((x / Math.cos(phi) + Math.log(settings.plot_axis_rmin) / Math.LN10) * Math.LN10);
        }
        else if (settings.plot_axis_rmin > settings.plot_axis_rmax) {
            r = settings.plot_axis_rmin - x / Math.cos(phi);
        } else {
            r = settings.plot_axis_rmin + x / Math.cos(phi);
        }
        phi = phi * (180 / Math.PI);
        if (settings.polar_sense < 0) {
            phi = -phi;
        }
        if (settings.polar_theta0 !== undefined) {
            phi = phi + settings.polar_theta0;
        }
        if (phi > 180) { phi = phi - 360; }
        polar.r = r;
        polar.ang = phi;
        return polar;
    };

    // Convert position to DMS
    var convertToDMS = function (x) {
        var dms = { d: 0, m: 0, s: 0 };
        var deg = Math.abs(x);
        dms.d = Math.floor(deg);
        dms.m = Math.floor((deg - dms.d) * 60);
        dms.s = Math.floor((deg - dms.d) * 3600 - dms.m * 60);
        fmt = ((x < 0) ? '-' : ' ') + dms.d.toFixed(0) + '°' + dms.m.toFixed(0) + '"' + dms.s.toFixed(0) + "'";
        return fmt;
    };

    // Set popover text to show
    var setPopoverText = function (content) {

        // Minimum length
        popoverText.width = popoverText.defaultWidth;

        // Remove old texts
        while (null !== popoverText.element.firstChild) {
            popoverText.element.removeChild(popoverText.element.firstChild);
        }

        var lines = content.split(/\n|\\n/g);

        // Single line
        if (lines.length <= 1) {
            popoverText.element.textContent = content;
            popoverText.width = popoverText.element.getComputedTextLength() + 8;
        }
        // Multiple lines
        else {
            var lineWidth = 0;
            var tspanElement;

            for (var l = 0; l < lines.length; l++) {
                tspanElement = document.createElementNS('http://www.w3.org/2000/svg', 'tspan');
                // Y relative position
                if (l > 0) {
                    tspanElement.setAttribute('dy', popoverText.defaultHeight);
                }
                // Append text
                tspanElement.appendChild(document.createTextNode(lines[l]));
                popoverText.element.appendChild(tspanElement);

                // Max line width
                lineWidth = tspanElement.getComputedTextLength() + 8;
                if (popoverText.width < lineWidth) {
                    popoverText.width = lineWidth;
                }
            }
        }

        // Box Height
        popoverText.height = 2 + popoverText.defaultHeight * lines.length;
        popoverContainer.element.setAttribute('height', popoverText.height);

        // Box Width
        popoverContainer.element.setAttribute('width', popoverText.width);
    };

    // Set popover image to show
    var setPopoverImage = function (content) {

        // Set default image size
        popoverImage.width = popoverImage.defaultWidth;
        popoverImage.height = popoverImage.defaultHeight;

        // Pick up height and width from image(width,height):name
        if (content.charAt(5) == '(') {
            popoverImage.width = parseInt(content.slice(6));
            popoverImage.height = parseInt(content.slice(content.indexOf(',') + 1));
        }

        popoverImage.element.setAttribute('width', popoverImage.width);
        popoverImage.element.setAttribute('height', popoverImage.height);
        popoverImage.element.setAttribute('preserveAspectRatio', 'none');

        // attach image URL as a link
        content = content.slice(content.indexOf(':') + 1);
        popoverImage.element.setAttributeNS('http://www.w3.org/1999/xlink', 'xlink:href', content);
    };

    // Show popover text in given position
    var showPopoverText = function (position) {
        var domRect = svgElement.getBoundingClientRect();
        domRect = convertDOMToSVG({'x': domRect.right, 'y': domRect.bottom });

        // bounce off frame bottom
        if (position.y + popoverText.height + 16 > domRect.y) {
            position.y = domRect.y - popoverText.height - 16;
        }

        // bounce off right edge
        if (position.x + popoverText.width + 14 > domRect.x) {
            position.x = domRect.x - popoverText.width - 14;
        }

        // Change Box position
        popoverContainer.element.setAttribute('x', position.x + 10);
        popoverContainer.element.setAttribute('y', position.y + 4);
        popoverContainer.element.setAttribute('visibility', 'visible');

        // Change Text position
        popoverText.element.setAttribute('x', position.x + 14);
        popoverText.element.setAttribute('y', position.y + 18);
        popoverText.element.setAttribute('visibility', 'visible');

        // Change multiline text position
        var tspan = popoverText.element.querySelectorAll('tspan');
        for (var i = 0; i < tspan.length; i++) {
            tspan[i].setAttribute('x', position.x + 14);
        }

        // Font properties
        if (settings.hypertext_fontFamily != null)
            popoverText.element.setAttribute('font-family', settings.hypertext_fontFamily);
        if (settings.hypertext_fontStyle != null)
            popoverText.element.setAttribute('font-style', settings.hypertext_fontStyle);
        if (settings.hypertext_fontWeight != null)
            popoverText.element.setAttribute('font-weight', settings.hypertext_fontWeight);
        if (settings.hypertext_fontSize > 0)
            popoverText.element.setAttribute('font-size', settings.hypertext_fontSize);
    };

    // Show popover image in given position
    var showPopoverImage = function (position) {
        var domRect = svgElement.getBoundingClientRect();
        domRect = convertDOMToSVG({'x': domRect.right, 'y': domRect.bottom });

        // bounce off frame bottom
        if (position.y + popoverImage.height + 16 > domRect.y) {
            position.y = domRect.y - popoverImage.height - 16;
        }

        // bounce off right edge
        if (position.x + popoverImage.width + 14 > domRect.x) {
            position.x = domRect.x - popoverImage.width - 14;
        }

        popoverImage.element.setAttribute('x', position.x);
        popoverImage.element.setAttribute('y', position.y);
        popoverImage.element.setAttribute('visibility', 'visible');
    };

    // Hide all popovers
    var hidePopover = function () {
        popoverContainer.element.setAttribute('visibility', 'hidden');
        popoverText.element.setAttribute('visibility', 'hidden');
        popoverImage.element.setAttribute('visibility', 'hidden');
    };

    // Zoom svg inside viewbox
    var zoom = function (direction) {
        var zoomRate = 1.1;
        var viewBoxValues = getViewBox();

        var widthBefore = viewBoxValues[2];
        var heightBefore = viewBoxValues[3];

        if (direction == 'in') {
            viewBoxValues[2] /= zoomRate;
            viewBoxValues[3] /= zoomRate;
            // Pan to center
            viewBoxValues[0] -= (viewBoxValues[2] - widthBefore) / 2;
            viewBoxValues[1] -= (viewBoxValues[3] - heightBefore) / 2;
        }
        else if (direction == 'out') {
            viewBoxValues[2] *= zoomRate;
            viewBoxValues[3] *= zoomRate;
            // Pan to center
            viewBoxValues[0] += (widthBefore - viewBoxValues[2]) / 2;
            viewBoxValues[1] += (heightBefore - viewBoxValues[3]) / 2;
        }

        return viewBoxValues;
    };

    // Pan svg inside viewbox
    var pan = function (direction) {
        var panRate = 10;
        var viewBoxValues = getViewBox();

        switch (direction) {
            case 'left':
                viewBoxValues[0] += panRate;
                break;
            case 'right':
                viewBoxValues[0] -= panRate;
                break;
            case 'up':
                viewBoxValues[1] += panRate;
                break;
            case 'down':
                viewBoxValues[1] -= panRate;
                break;
        }

        return viewBoxValues;
    };

    // Toggle key and chart on/off or set manually to wanted
    var key = function (id, set, evt) {
        var visibility = null;

        // Chart element
        var chartElement = svgElement.getElementById(id.replace('_keyentry', ''));
        if (chartElement) {
            // Set on/off
            if (set === true || set === false) {
                visibility = set ? 'visible' : 'hidden';
            }
            // Toggle
            else {
                visibility = chartElement.getAttribute('visibility') == 'hidden' ? 'visible' : 'hidden';
            }
            chartElement.setAttribute('visibility', visibility);
        }

        // Key element
        var keyElement = svgElement.getElementById(id);
        if (keyElement && visibility) {
            keyElement.setAttribute('style', visibility == 'hidden' ? 'filter:url(#greybox)' : 'none');
        }

        if (evt !== undefined) {
            evt.stopPropagation();
            evt.preventDefault();
        }
    };

    // Toggle coordinates on/off or set manually to wanted
    var coordinate = function (set) {
        if (coordinateText.element) {
            // Set on/off
            if (set === true || set === false) {
                coordinateText.enabled = set;
            }
            // Toggle
            else {
                coordinateText.enabled = coordinateText.element.getAttribute('visibility') == 'hidden' ? true : false;
            }
            coordinateText.element.setAttribute('visibility', coordinateText.enabled ? 'visible' : 'hidden');
        }
    };

    // Toggle grid on/off or set manually to wanted
    var grid = function (set) {
        var grid = svgElement.getElementsByClassName('gridline');

        // Set on/off
        if (set === true || set === false) {
            gridEnabled = set;
        }
        // Toggle, get state from first element
        else if (grid.length) {
            gridEnabled = grid[0].getAttribute('visibility') == 'hidden' ? true : false;
        }

        for (i = 0; i < grid.length; i++) {
            grid[i].setAttribute('visibility', gridEnabled ? 'visible' : 'hidden');
        }
    };

    // Show popover text or image
    var popover = function (content, set, evt) {

        // Hide popover
        if (set === false) {
            hidePopover();

            if (evt !== undefined) {
                evt.stopPropagation();
                evt.preventDefault();
            }

            return;
        }

        var position = null;

        // Change content only if changed
        if (popoverContainer.content != content) {

            // Set current text
            popoverContainer.content = content;

            popoverImage.content = '';
            popoverText.content = content;

            // If text starts with image: process it as an xlinked bitmap
            if (content.substring(0, 5) == 'image') {
                var lines = content.split(/\n|\\n/g);
                var nameindex = lines[0].indexOf(':');
                if (nameindex > 0) {
                    popoverImage.content = lines.shift();
                    popoverText.content = '';

                    // Additional text lines
                    if (lines !== undefined && lines.length > 0) {
                        popoverText.content = lines.join('\n');
                    }
                }
            }

            // Set image content
            if(popoverImage.content){
                setPopoverImage(popoverImage.content);
            }

            // Set text content
            if(popoverText.content){
                setPopoverText(popoverText.content);
            }
        }

        if(popoverImage.content || popoverText.content){
            position = convertDOMToSVG({'x': evt.clientX, 'y': evt.clientY });
        }

        // Show popover image on mouse position
        if(popoverImage.content){
            showPopoverImage(position);
        }

        // Show popover on mouse position
        if(popoverText.content){
            showPopoverText(position);
        }

        if (evt !== undefined) {
            evt.stopPropagation();
            evt.preventDefault();
        }
    };

    // Parse plot settings
    parseSettings();

    // viewBox initial position and size
    viewBoxResetValue = getViewBox();

    // Set focusable for event focusing, not work on old svg version
    svgElement.setAttribute('focusable', true);

    // Disable native draggable
    svgElement.setAttribute('draggable', false);

    // Add events
    addEvents();

    // Return functions to outside use
    return {
        zoom: function (direction) {
            setViewBox(zoom(direction));
            return this;
        },
        pan: function (direction) {
            setViewBox(pan(direction));
            return this;
        },
        reset: function () {
            setViewBox(viewBoxResetValue);
            return this;
        },
        key: function (id, set) {
            key(id, set);
            return this;
        },
        coordinate: function (set) {
            coordinate(set);
            return this;
        },
        grid: function (set) {
            grid(set);
            return this;
        }
    };
};

// Old init function, remove when svg inline events removed
gnuplot_svg.Init = function() { };
]]>
</script>

<!-- Tie mousing to entire bounding box of the plot -->
<rect x="0" y="0" width="600" height="480" fill="#ffffff" stroke="black" stroke-width="1"
onclick="gnuplot_svg.toggleCoordBox(evt)"  onmousemove="gnuplot_svg.moveCoordBox(evt)"/>

<!-- Also track mouse when it is on a plot element -->
<g id="gnuplot_canvas" onclick="gnuplot_svg.toggleCoordBox(evt)" onmousemove="gnuplot_svg.moveCoordBox(evt)">

<defs>

	<circle id='gpDot' r='0.5' stroke-width='0.5' stroke='currentColor'/>
	<path id='gpPt0' stroke-width='0.222' stroke='currentColor' d='M-1,0 h2 M0,-1 v2'/>
	<path id='gpPt1' stroke-width='0.222' stroke='currentColor' d='M-1,-1 L1,1 M1,-1 L-1,1'/>
	<path id='gpPt2' stroke-width='0.222' stroke='currentColor' d='M-1,0 L1,0 M0,-1 L0,1 M-1,-1 L1,1 M-1,1 L1,-1'/>
	<rect id='gpPt3' stroke-width='0.222' stroke='currentColor' x='-1' y='-1' width='2' height='2'/>
	<rect id='gpPt4' stroke-width='0.222' stroke='currentColor' fill='currentColor' x='-1' y='-1' width='2' height='2'/>
	<circle id='gpPt5' stroke-width='0.222' stroke='currentColor' cx='0' cy='0' r='1'/>
	<use xlink:href='#gpPt5' id='gpPt6' fill='currentColor' stroke='none'/>
	<path id='gpPt7' stroke-width='0.222' stroke='currentColor' d='M0,-1.33 L-1.33,0.67 L1.33,0.67 z'/>
	<use xlink:href='#gpPt7' id='gpPt8' fill='currentColor' stroke='none'/>
	<use xlink:href='#gpPt7' id='gpPt9' stroke='currentColor' transform='rotate(180)'/>
	<use xlink:href='#gpPt9' id='gpPt10' fill='currentColor' stroke='none'/>
	<use xlink:href='#gpPt3' id='gpPt11' stroke='currentColor' transform='rotate(45)'/>
	<use xlink:href='#gpPt11' id='gpPt12' fill='currentColor' stroke='none'/>
	<path id='gpPt13' stroke-width='0.222' stroke='currentColor' d='M0,1.330 L1.265,0.411 L0.782,-1.067 L-0.782,-1.076 L-1.265,0.411 z'/>
	<use xlink:href='#gpPt13' id='gpPt14' fill='currentColor' stroke='none'/>
	<filter id='textbox' filterUnits='objectBoundingBox' x='0' y='0' height='1' width='1'>
	  <feFlood flood-color='#FFFFFF' flood-opacity='1' result='bgnd'/>
	  <feComposite in='SourceGraphic' in2='bgnd' operator='atop'/>
	</filter>
	<filter id='greybox' filterUnits='objectBoundingBox' x='0' y='0' height='1' width='1'>
	  <feFlood flood-color='lightgrey' flood-opacity='1' result='grey'/>
	  <feComposite in='SourceGraphic' in2='grey' operator='atop'/>
	</filter>
</defs>
<g fill="none" color="#FFFFFF" stroke="black" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M62.92,444.00 L71.92,444.00 M574.82,444.00 L565.82,444.00  '/>	<g transform="translate(54.53,447.90)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" > 0.01</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M62.92,379.88 L67.42,379.88 M574.82,379.88 L570.32,379.88 M62.92,342.38 L67.42,342.38 M574.82,342.38 L570.32,342.38
		M62.92,315.76 L67.42,315.76 M574.82,315.76 L570.32,315.76 M62.92,295.12 L67.42,295.12 M574.82,295.12 L570.32,295.12
		M62.92,278.26 L67.42,278.26 M574.82,278.26 L570.32,278.26 M62.92,264.00 L67.42,264.00 M574.82,264.00 L570.32,264.00
		M62.92,251.65 L67.42,251.65 M574.82,251.65 L570.32,251.65 M62.92,240.75 L67.42,240.75 M574.82,240.75 L570.32,240.75
		M62.92,231.00 L71.92,231.00 M574.82,231.00 L565.82,231.00  '/>	<g transform="translate(54.53,234.90)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" > 0.1</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M62.92,166.89 L67.42,166.89 M574.82,166.89 L570.32,166.89 M62.92,129.38 L67.42,129.38 M574.82,129.38 L570.32,129.38
		M62.92,102.77 L67.42,102.77 M574.82,102.77 L570.32,102.77 M62.92,82.13 L67.42,82.13 M574.82,82.13 L570.32,82.13
		M62.92,65.26 L67.42,65.26 M574.82,65.26 L570.32,65.26 M62.92,51.00 L67.42,51.00 M574.82,51.00 L570.32,51.00
		M62.92,38.65 L67.42,38.65 M574.82,38.65 L570.32,38.65 M62.92,27.76 L67.42,27.76 M574.82,27.76 L570.32,27.76
		M62.92,18.01 L71.92,18.01 M574.82,18.01 L565.82,18.01  '/>	<g transform="translate(54.53,21.91)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" > 1</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M69.28,444.00 L69.28,439.50 M69.28,18.01 L69.28,22.51 M89.64,444.00 L89.64,435.00 M89.64,18.01 L89.64,27.01
		 '/>	<g transform="translate(89.64,465.90)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="middle">
		<text><tspan font-family="Arial" > 0.01</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M223.56,444.00 L223.56,439.50 M223.56,18.01 L223.56,22.51 M301.90,444.00 L301.90,439.50 M301.90,18.01 L301.90,22.51
		M357.49,444.00 L357.49,439.50 M357.49,18.01 L357.49,22.51 M400.60,444.00 L400.60,439.50 M400.60,18.01 L400.60,22.51
		M435.83,444.00 L435.83,439.50 M435.83,18.01 L435.83,22.51 M465.61,444.00 L465.61,439.50 M465.61,18.01 L465.61,22.51
		M491.41,444.00 L491.41,439.50 M491.41,18.01 L491.41,22.51 M514.17,444.00 L514.17,439.50 M514.17,18.01 L514.17,22.51
		M534.53,444.00 L534.53,435.00 M534.53,18.01 L534.53,27.01  '/>	<g transform="translate(534.53,465.90)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="middle">
		<text><tspan font-family="Arial" > 0.1</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M62.92,18.01 L62.92,444.00 L574.82,444.00 L574.82,18.01 L62.92,18.01 Z  '/></g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>
	<g id="gnuplot_plot_1" ><title>original power law</title>
<g fill="none" color="#FFFFFF" stroke="black" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<g id="gnuplot_plot_1_keyentry" visibility="visible" onclick="gnuplot_svg.toggleVisibility(evt,'gnuplot_plot_1')">
	<g transform="translate(222.33,39.91)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" >original power law</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='rgb(228,  26,  28)'  d='M230.72,36.01 L273.28,36.01  '/>	</g>
	<path stroke='rgb(228,  26,  28)'  d='M89.64,316.20 L101.05,309.65 L112.45,303.10 L123.86,296.54 L135.27,289.99 L146.68,283.43 L158.08,276.88 L169.49,270.33
		L180.90,263.77 L192.31,257.22 L203.71,250.67 L215.12,244.11 L226.53,237.56 L237.94,231.00 L249.34,224.45 L260.75,217.90
		L272.16,211.34 L283.56,204.79 L294.97,198.24 L306.38,191.68 L317.79,185.13 L329.19,178.58 L340.60,172.02 L352.01,165.47
		L363.42,158.91 L374.82,152.36 L386.23,145.81 L397.64,139.25 L409.05,132.70 L420.45,126.15 L431.86,119.59 L443.27,113.04
		L454.68,106.48 L466.08,99.93 L477.49,93.38 L488.90,86.82 L500.30,80.27 L511.71,73.72 L523.12,67.16 L534.53,60.61
		 '/>	<use xlink:href='#gpPt0' transform='translate(89.64,316.20) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(101.05,309.65) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(112.45,303.10) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(123.86,296.54) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(135.27,289.99) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(146.68,283.43) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(158.08,276.88) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(169.49,270.33) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(180.90,263.77) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(192.31,257.22) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(203.71,250.67) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(215.12,244.11) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(226.53,237.56) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(237.94,231.00) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(249.34,224.45) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(260.75,217.90) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(272.16,211.34) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(283.56,204.79) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(294.97,198.24) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(306.38,191.68) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(317.79,185.13) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(329.19,178.58) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(340.60,172.02) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(352.01,165.47) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(363.42,158.91) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(374.82,152.36) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(386.23,145.81) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(397.64,139.25) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(409.05,132.70) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(420.45,126.15) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(431.86,119.59) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(443.27,113.04) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(454.68,106.48) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(466.08,99.93) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(477.49,93.38) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(488.90,86.82) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(500.30,80.27) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(511.71,73.72) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(523.12,67.16) scale(4.50)' color='rgb(228,  26,  28)'/>
	<use xlink:href='#gpPt0' transform='translate(534.53,60.61) scale(4.50)' color='rgb(228,  26,  28)'/>
	<g id="gnuplot_plot_1_keyentry" visibility="visible" onclick="gnuplot_svg.toggleVisibility(evt,'gnuplot_plot_1')">
	<use xlink:href='#gpPt0' transform='translate(252.00,36.01) scale(4.50)' color='rgb(228,  26,  28)'/>
	</g>
</g>
	</g>
	<g id="gnuplot_plot_2" ><title>noisy power law</title>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<g id="gnuplot_plot_2_keyentry" visibility="visible" onclick="gnuplot_svg.toggleVisibility(evt,'gnuplot_plot_2')">
	<g transform="translate(222.33,57.91)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" >noisy power law</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	</g>
	<use xlink:href='#gpPt1' transform='translate(110.93,362.89) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(94.03,318.25) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(62.92,312.09) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(166.80,284.95) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(162.42,305.73) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(110.87,279.99) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(155.07,253.14) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(171.19,277.42) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(213.29,275.89) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(165.26,241.21) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(230.84,243.67) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(254.12,257.63) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(271.46,229.76) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(164.51,242.30) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(244.94,221.30) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(247.47,216.37) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(255.49,203.75) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(292.36,206.49) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(329.70,179.49) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(305.65,210.55) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(381.54,176.73) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(319.05,177.23) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(274.95,154.50) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(324.44,156.93) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(343.18,179.51) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(370.68,148.71) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(413.09,162.34) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(423.94,132.25) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(437.82,116.07) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(407.35,127.74) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(358.98,136.05) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(504.15,104.57) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(429.34,105.22) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(523.64,123.18) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(422.34,83.48) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(479.97,96.76) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(495.89,61.38) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(480.21,66.39) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(557.65,88.54) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<use xlink:href='#gpPt1' transform='translate(574.82,57.32) scale(4.50)' color='rgb( 55, 126, 184)'/>
	<g id="gnuplot_plot_2_keyentry" visibility="visible" onclick="gnuplot_svg.toggleVisibility(evt,'gnuplot_plot_2')">
	<use xlink:href='#gpPt1' transform='translate(252.00,54.01) scale(4.50)' color='rgb( 55, 126, 184)'/>
	</g>
</g>
	</g>
	<g id="gnuplot_plot_3" ><title>log-linear fit</title>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<g id="gnuplot_plot_3_keyentry" visibility="visible" onclick="gnuplot_svg.toggleVisibility(evt,'gnuplot_plot_3')">
	<g transform="translate(222.33,75.91)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" >log-linear fit</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='rgb( 77, 175,  74)'  d='M230.72,72.01 L273.28,72.01  '/>	</g>
	<path stroke='rgb( 77, 175,  74)'  d='M110.93,299.77 L94.03,308.99 L62.92,325.95 L166.80,269.32 L162.42,271.70 L110.87,299.80 L155.07,275.71 L171.19,266.92
		L213.29,243.97 L165.26,270.15 L230.84,234.40 L254.12,221.71 L271.46,212.26 L164.51,270.56 L244.94,226.71 L247.47,225.33
		L255.49,220.96 L292.36,200.86 L329.70,180.51 L305.65,193.62 L381.54,152.25 L319.05,186.31 L274.95,210.36 L324.44,183.38
		L343.18,173.16 L370.68,158.16 L413.09,135.05 L423.94,129.13 L437.82,121.56 L407.35,138.18 L358.98,164.54 L504.15,85.40
		L429.34,126.19 L523.64,74.78 L422.34,130.00 L479.97,98.59 L495.89,89.90 L480.21,98.45 L557.65,56.24 L574.82,46.88
		 '/></g>
	</g>
	<g id="gnuplot_plot_4" ><title>nonlinear fit</title>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<g id="gnuplot_plot_4_keyentry" visibility="visible" onclick="gnuplot_svg.toggleVisibility(evt,'gnuplot_plot_4')">
	<g transform="translate(222.33,93.91)" stroke="none" fill="black" font-family="Arial" font-size="12.00"  text-anchor="end">
		<text><tspan font-family="Arial" >nonlinear fit</tspan></text>
	</g>
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='rgb(152,  78, 163)'  d='M230.72,90.01 L273.28,90.01  '/>	</g>
	<path stroke='rgb(152,  78, 163)'  d='M110.93,268.94 L94.03,276.58 L62.92,290.66 L166.80,243.66 L162.42,245.64 L110.87,268.96 L155.07,248.96 L171.19,241.67
		L213.29,222.62 L165.26,244.35 L230.84,214.68 L254.12,204.14 L271.46,196.29 L164.51,244.69 L244.94,208.30 L247.47,207.15
		L255.49,203.52 L292.36,186.84 L329.70,169.94 L305.65,180.83 L381.54,146.49 L319.05,174.76 L274.95,194.72 L324.44,172.32
		L343.18,163.84 L370.68,151.40 L413.09,132.21 L423.94,127.30 L437.82,121.02 L407.35,134.81 L358.98,156.69 L504.15,91.00
		L429.34,124.85 L523.64,82.18 L422.34,128.02 L479.97,101.95 L495.89,94.74 L480.21,101.84 L557.65,66.80 L574.82,59.03
		 '/></g>
	</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="2.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>
<g fill="none" color="black" stroke="black" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
	<path stroke='black'  d='M62.92,18.01 L62.92,444.00 L574.82,444.00 L574.82,18.01 L62.92,18.01 Z  '/></g>
<g fill="none" color="black" stroke="currentColor" stroke-width="1.00" stroke-linecap="butt" stroke-linejoin="miter">
</g>

<script type="text/javascript"><![CDATA[
// plot boundaries and axis scaling information for mousing 
gnuplot_svg.plot_term_xmax = 600;
gnuplot_svg.plot_term_ymax = 480;
gnuplot_svg.plot_xmin = 62.9;
gnuplot_svg.plot_xmax = 574.8;
gnuplot_svg.plot_ybot = 444.0;
gnuplot_svg.plot_ytop = 18.0;
gnuplot_svg.plot_width = 511.9;
gnuplot_svg.plot_height = 426.0;
gnuplot_svg.plot_axis_xmin = 0.00870843;
gnuplot_svg.plot_axis_xmax = 0.123188;
gnuplot_svg.plot_axis_ymin = 0.01;
gnuplot_svg.plot_axis_ymax = 1;
gnuplot_svg.polar_mode = false;
gnuplot_svg.plot_axis_x2min = "none";
gnuplot_svg.plot_axis_y2min = "none";
gnuplot_svg.plot_logaxis_x = 1;
gnuplot_svg.plot_logaxis_y = 1;
gnuplot_svg.plot_timeaxis_x = "";
gnuplot_svg.plot_timeaxis_y = "";
gnuplot_svg.hypertext_fontSize = 0;
gnuplot_svg.hypertext_fontName = null;
gnuplot_svg.hypertext_fontStyle = null;
gnuplot_svg.hypertext_fontWeight = null;
]]>
</script>
</g>

  <text id="coord_text" text-anchor="start" pointer-events="none"
  font-size="12" font-family="Arial"
  visibility="hidden"> </text>

  <rect id="hypertextbox" class="hypertextbox" pointer-events="none"
  fill="white" stroke="black" opacity="0.8"
  height="16" visibility="hidden" />

  <text id="hypertext" class="hypertext" pointer-events="none"
  font-size="12" font-family="Arial"
  visibility="hidden"> </text>

  <image id="hyperimage" class="hyperimage" pointer-events="none"
  fill="white" stroke="black" opacity="0.8"
  height="200" width="300" visibility="hidden" />
</svg>

